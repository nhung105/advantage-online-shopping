// type RegisterForm = { username?: string; email: string, password: string };

// function register({ email, ...rest }: RegisterForm) {
//   console.log(email);
//   console.log(rest.password);
// } // {} => object

// register({ email: "" });

// truthy vs falsy => undefined, 0, null, false,...
// undefined && true;
